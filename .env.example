# En développement : /api (proxy Vite vers le backend)
VITE_API_URL=/api

# En production : URL complète du backend (obligatoire pour le build déployé)
# Exemple (remplacez par votre URL) :
# VITE_API_URL=https://samaterrainbackend.onrender.com
